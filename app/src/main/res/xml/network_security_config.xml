<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Default config allows cleartext only for debug but we keep minimal config here. -->
    <base-config cleartextTrafficPermitted="false" />
</network-security-config>
